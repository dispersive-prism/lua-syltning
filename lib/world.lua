World = {
    -- Gravity negative due to fact that y axis is inverted
    gravity = -3.5,
    drag = 800,
    scrollSpeedX = 3,
    scrollSpeedY = 3,
    allowedXDelta = 10,
    allowedYDelta = 10
}

return World